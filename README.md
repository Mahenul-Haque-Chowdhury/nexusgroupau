
  # Luxury Cinematic Website Design (Next.js)

  This project has been migrated to Next.js (App Router) while preserving the existing page and motion behavior.

  ## Running the code

  Run `npm install` to install dependencies.

  Run `npm run dev` to start the Next.js development server.

  Build for production with `npm run build` and run production mode with `npm run start`.
  